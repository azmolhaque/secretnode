fastapi==0.141.1
uvicorn[standard]==0.52.4
uvloop==0.22.1
httpx==0.28.1
h2==4.4.1
brotli==1.2.0
# Capped below 17.0 by google-genai (websockets<17.0,>=13.0.0). uvicorn[standard]
# only needs >=13.0, so google-genai is the binding constraint here.
websockets==16.1.1
beautifulsoup4==4.15.0
lxml==6.1.3
aiosqlite==0.22.1
google-genai==2.22.0
pydantic==2.13.5
python-dotenv==1.2.3

# ── dev/test only (not needed in production) ──────────────────────────────
# pip install -r requirements.txt --break-system-packages
# then: pytest backend/tests/ -v
pytest==9.1.1
pytest-asyncio==1.4.0
